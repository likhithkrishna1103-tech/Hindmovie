import { ELFOpenResult } from "./types";
import { Reader } from "./reader";
/** Options for reading an ELF file. */
export interface OpenOptions {
    /** When true, the data for symbols will also be read */
    readSymbolData: boolean;
}
export declare function readElf(reader: Reader, options: OpenOptions): Promise<ELFOpenResult>;
