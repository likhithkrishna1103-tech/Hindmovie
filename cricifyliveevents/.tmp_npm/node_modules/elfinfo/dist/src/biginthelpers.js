"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toNumberSafe = exports.divide = exports.add = exports.subtract = void 0;
function subtract(a, b) {
    if (typeof a == 'bigint' && typeof b == 'bigint') {
        return a - b;
    }
    else if (typeof a == 'number' && typeof b == 'number') {
        return a - b;
    }
    else {
        return BigInt(a) - BigInt(b);
    }
}
exports.subtract = subtract;
function add(a, b) {
    if (typeof a == 'bigint' && typeof b == 'bigint') {
        return a + b;
    }
    else if (typeof a == 'number' && typeof b == 'number') {
        return a + b;
    }
    else {
        return BigInt(a) + BigInt(b);
    }
}
exports.add = add;
function divide(a, b) {
    if (typeof a == 'bigint' && typeof b == 'bigint') {
        return a / b;
    }
    else if (typeof a == 'number' && typeof b == 'number') {
        return parseInt((a / b));
    }
    else {
        return BigInt(a) / BigInt(b);
    }
}
exports.divide = divide;
const tooBigInt = BigInt(1e51);
function toNumberSafe(a, warnings) {
    if (typeof a === 'bigint') {
        if (a > tooBigInt) {
            if (warnings) {
                warnings.push('BigInt Overflow');
            }
            else {
                throw new Error('BigInt Overflow');
            }
        }
        else {
            return Number(a);
        }
    }
    else if (typeof a === 'number') {
        return a;
    }
    throw new Error('Invalid Input for BigInt conversion');
}
exports.toNumberSafe = toNumberSafe;
//# sourceMappingURL=biginthelpers.js.map