import { ELFSegment } from './types';
import { Reader } from './reader';
export declare function readProgramHeaderEntries(fh: Reader, ph_off: number | bigint, ph_entsize: number, ph_num: number, bits: number, bigEndian: boolean): Promise<ELFSegment[]>;
