#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const _1 = require("./");
const debug_1 = require("./debug");
(async function main() {
    const programs = process.argv.slice(2);
    if (programs.length) {
        for (const program of programs) {
            const elf = await _1.open(program);
            console.log(debug_1.debug(elf));
            console.log('\n\n');
        }
    }
    else {
        console.log(`Usage: elfinfo [program]`);
    }
})().catch(console.error);
//# sourceMappingURL=elfinfo.js.map