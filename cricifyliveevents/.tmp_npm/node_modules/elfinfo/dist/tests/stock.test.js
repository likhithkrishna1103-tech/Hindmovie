"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const elfinfo = __importStar(require("../src"));
const testprograms_1 = require("./testprograms");
const _1 = require("./");
_1.category("Stock Programs", async () => {
    await Promise.all(testprograms_1.stockProgramNames.flatMap(name => testprograms_1.stockProgramAbis.map(async (abi) => {
        const { program } = testprograms_1.stockPrograms[name][abi];
        const elf = await elfinfo.open(program);
        elf.warnings.forEach(w => console.warn(w));
        elf.errors.forEach(e => console.error(e));
        _1.test(`${name} (${abi}) should open without problems`, async () => {
            assert_1.default(elf.success === true);
            assert_1.default(elf.errors.length === 0);
            assert_1.default(elf.warnings.length === 0);
        });
        _1.test(`${name} (${abi}) should have sections`, async () => {
            assert_1.default(elf.elf);
            assert_1.default(elf.elf.sections.length > 0);
        });
        _1.test(`${name} (${abi}) should have segments`, async () => {
            assert_1.default(elf.elf);
            assert_1.default(elf.elf.segments.length > 0);
        });
        const symbols = elf.elf && elfinfo.getSymbols(elf.elf);
        _1.test(`${name} (${abi}) should have symbols`, async () => {
            assert_1.default(symbols);
        });
        _1.test(`${name} (${abi}) should have a main symbol`, async () => {
            let main_symbol_found = false;
            assert_1.default(symbols);
            for (const symbol of symbols) {
                if (/_?main/.test(symbol.name)) {
                    main_symbol_found = true;
                    break;
                }
            }
            assert_1.default(main_symbol_found);
        });
    })));
});
//# sourceMappingURL=stock.test.js.map