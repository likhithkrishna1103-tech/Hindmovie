"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const src_1 = require("../src");
const testprograms_1 = require("./testprograms");
const _1 = require("./");
async function load(data) {
    const elf = await src_1.open(data);
    elf.warnings.forEach(w => console.warn(w));
    elf.errors.forEach(e => console.error(e));
    assert_1.default(elf);
    assert_1.default(elf.elf);
    assert_1.default(elf.success === true);
    return elf.elf;
}
_1.category("Functions", () => {
    const elfdata = testprograms_1.testPrograms['clang-x64'].program;
    const armelfdata = testprograms_1.testPrograms['arm-eabi'].program;
    _1.test(`getSymbols works`, async () => {
        const elf = await load(elfdata);
        const allsyms = src_1.getSymbols(elf);
        assert_1.default(allsyms);
        assert_1.default(allsyms.length > 0);
        assert_1.default(allsyms.filter(x => x.name == 'main.cpp').length === 1);
        assert_1.default(allsyms.filter(x => x.name == '__init_array_end').length === 1);
        assert_1.default(allsyms.filter(x => x.name == '_Z10factorialff').length === 1);
        assert_1.default(allsyms.filter(x => x.name == 'printf').length === 1);
    });
    _1.test(`getSymbolsInSection (section) works`, async () => {
        const elf = await load(elfdata);
        const section = src_1.getSectionByName(elf, '.text');
        const syms = src_1.getSymbolsInSection(elf, section);
        assert_1.default(syms);
        assert_1.default(syms.length > 0);
        assert_1.default(syms.filter(x => x.name == '_Z10factorialff').length !== 0);
    });
    _1.test(`getSymbolsInSection (index) works`, async () => {
        const elf = await load(elfdata);
        const section = src_1.getSectionByName(elf, '.text');
        const syms = src_1.getSymbolsInSection(elf, section.index);
        assert_1.default(syms);
        assert_1.default(syms.length > 0);
        assert_1.default(syms.filter(x => x.name == 'main').length !== 0);
    });
    _1.test(`getSymbolsInSegment (segment) works`, async () => {
        // TODO: this will likely break between compilers
        const elf = await load(elfdata);
        const syms = src_1.getSymbolsInSegment(elf, elf.segments[3]);
        assert_1.default(syms);
        assert_1.default(syms.length > 0);
        assert_1.default(syms.filter(x => x.name == 'main').length !== 0);
    });
    _1.test(`getSymbolsInSegment (index) works`, async () => {
        const elf = await load(elfdata);
        const syms = src_1.getSymbolsInSegment(elf, 3);
        assert_1.default(syms);
        assert_1.default(syms.length > 0);
        assert_1.default(syms.filter(x => x.name == '_Z10factorialff').length !== 0);
    });
    _1.test(`getSectionsInSegment works`, async () => {
        const elf = await load(elfdata);
        const sections1 = src_1.getSectionsInSegment(elf, 3);
        assert_1.default(sections1);
        assert_1.default(sections1.length > 0);
        assert_1.default(sections1.filter(x => x.name == '.text').length !== 0);
        const sections2 = src_1.getSectionsInSegment(elf, 5);
        assert_1.default(sections2);
        assert_1.default(sections2.length > 0);
        assert_1.default(sections2.filter(x => x.name == '.bss').length !== 0);
    });
    _1.test(`getSectionForSymbol works`, async () => {
        const elf = await load(elfdata);
        const symbol = src_1.getSymbolByName(elf, 'main');
        assert_1.default(symbol);
        const section = src_1.getSectionForSymbol(elf, symbol);
        assert_1.default(section);
        assert_1.default(section.name === '.text');
    });
    _1.test(`getSegmentForSymbol works`, async () => {
        const elf = await load(elfdata);
        const symbol = src_1.getSymbolByName(elf, 'main');
        assert_1.default(symbol);
        const segment = src_1.getSegmentForSymbol(elf, symbol);
        assert_1.default(segment);
    });
    _1.test(`getSymbolsAtVirtualMemoryLocation works when queried at start of symbol`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'errno');
        assert_1.default(symbol);
        const symbols = src_1.getSymbolsAtVirtualMemoryLocation(elf, Number(symbol.value));
        assert_1.default(symbols);
        assert_1.default(symbols.length > 0);
        assert_1.default(symbols.includes(symbol));
    });
    _1.test(`getSymbolsAtVirtualMemoryLocation works when queried in middle of symbol`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'errno');
        assert_1.default(symbol);
        const symbols = src_1.getSymbolsAtVirtualMemoryLocation(elf, Number(symbol.value) + 2);
        assert_1.default(symbols);
        assert_1.default(symbols.length > 0);
        assert_1.default(symbols.includes(symbol));
    });
    _1.test(`getSymbolsAtVirtualMemoryLocation works when queried on last byte of symbol`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'errno');
        assert_1.default(symbol);
        const symbols = src_1.getSymbolsAtVirtualMemoryLocation(elf, Number(symbol.value) + Number(symbol.size) - 1);
        assert_1.default(symbols);
        assert_1.default(symbols.length > 0);
        assert_1.default(symbols.includes(symbol));
    });
    _1.test(`getSymbolsAtVirtualMemoryLocation does not work when queried past end of symbol`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'errno');
        assert_1.default(symbol);
        const symbols = src_1.getSymbolsAtVirtualMemoryLocation(elf, Number(symbol.value) + Number(symbol.size));
        assert_1.default(symbols);
        assert_1.default(!(symbols.includes(symbol)));
    });
    _1.test(`getSymbolsAtVirtualMemoryLocation does not work when queried past beginning of symbol`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'errno');
        assert_1.default(symbol);
        const symbols = src_1.getSymbolsAtVirtualMemoryLocation(elf, Number(symbol.value) - 1);
        assert_1.default(symbols);
        assert_1.default(!(symbols.includes(symbol)));
    });
    _1.test(`getSymbolsAtPhysicalMemoryLocation works when queried at start of symbol`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'globaldatastring');
        assert_1.default(symbol);
        const symbolAddress = src_1.virtualAddressToPhysical(elf, symbol.value);
        assert_1.default(symbolAddress !== undefined);
        const symbols = src_1.getSymbolsAtPhysicalMemoryLocation(elf, symbolAddress);
        assert_1.default(symbols);
        assert_1.default(symbols.length > 0);
        assert_1.default(symbols.includes(symbol));
    });
    _1.test(`getSymbolsAtPhysicalMemoryLocation works when queried in middle of symbol`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'globaldatastring');
        assert_1.default(symbol);
        const symbolAddress = src_1.virtualAddressToPhysical(elf, symbol.value);
        const symbols = src_1.getSymbolsAtPhysicalMemoryLocation(elf, Number(symbolAddress) + 2);
        assert_1.default(symbols);
        assert_1.default(symbols.length > 0);
        assert_1.default(symbols.includes(symbol));
    });
    _1.test(`getSymbolsAtPhysicalMemoryLocation works when queried on last byte of symbol`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'globaldatastring');
        assert_1.default(symbol);
        const symbolAddress = src_1.virtualAddressToPhysical(elf, symbol.value);
        const symbols = src_1.getSymbolsAtPhysicalMemoryLocation(elf, Number(symbolAddress) + Number(symbol.size) - 1);
        assert_1.default(symbols);
        assert_1.default(symbols.length > 0);
        assert_1.default(symbols.includes(symbol));
    });
    _1.test(`getSymbolsAtPhysicalMemoryLocation does not work when queried past end of symbol`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'globaldatastring');
        assert_1.default(symbol);
        const symbolAddress = src_1.virtualAddressToPhysical(elf, symbol.value);
        const symbols = src_1.getSymbolsAtPhysicalMemoryLocation(elf, Number(symbolAddress) + Number(symbol.size));
        assert_1.default(symbols);
        assert_1.default(!(symbols.includes(symbol)));
    });
    _1.test(`getSymbolsAtPhysicalMemoryLocation does not work when queried past beginning of symbol`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'globaldatastring');
        assert_1.default(symbol);
        const symbolAddress = src_1.virtualAddressToPhysical(elf, symbol.value);
        const symbols = src_1.getSymbolsAtPhysicalMemoryLocation(elf, Number(symbolAddress) - 1);
        assert_1.default(symbols);
        assert_1.default(!(symbols.includes(symbol)));
    });
    _1.test(`getSectionsAtVirtualMemoryLocation works when queried at start of section`, async () => {
        const elf = await load(armelfdata);
        const section = src_1.getSectionByName(elf, '.text');
        const sections = src_1.getSectionsAtVirtualMemoryLocation(elf, Number(section.addr));
        assert_1.default(sections);
        assert_1.default(sections.includes(section));
    });
    _1.test(`getSectionsAtVirtualMemoryLocation works when queried in middle of section`, async () => {
        const elf = await load(armelfdata);
        const section = src_1.getSectionByName(elf, '.text');
        const sections = src_1.getSectionsAtVirtualMemoryLocation(elf, Number(section.addr) + 10);
        assert_1.default(sections);
        assert_1.default(sections.includes(section));
    });
    _1.test(`getSectionsAtVirtualMemoryLocation works when queried at end of section`, async () => {
        const elf = await load(armelfdata);
        const section = src_1.getSectionByName(elf, '.text');
        const sections = src_1.getSectionsAtVirtualMemoryLocation(elf, Number(section.addr) + Number(section.size) - 1);
        assert_1.default(sections);
        assert_1.default(sections.includes(section));
    });
    _1.test(`getSectionsAtPhysicalMemoryLocation works when queried at start of section`, async () => {
        const elf = await load(armelfdata);
        const section = src_1.getSectionByName(elf, '.text');
        const address = src_1.virtualAddressToPhysical(elf, section.addr);
        const sections = src_1.getSectionsAtPhysicalMemoryLocation(elf, Number(address));
        assert_1.default(sections);
        assert_1.default(sections.includes(section));
    });
    _1.test(`getSectionsAtPhysicalMemoryLocation works when queried in middle of section`, async () => {
        const elf = await load(armelfdata);
        const section = src_1.getSectionByName(elf, '.text');
        const address = src_1.virtualAddressToPhysical(elf, section.addr);
        const sections = src_1.getSectionsAtPhysicalMemoryLocation(elf, Number(address) + 10);
        assert_1.default(sections);
        assert_1.default(sections.includes(section));
    });
    _1.test(`getSectionsAtPhysicalMemoryLocation works when queried at end of section`, async () => {
        const elf = await load(armelfdata);
        const section = src_1.getSectionByName(elf, '.text');
        const address = src_1.virtualAddressToPhysical(elf, section.addr);
        const sections = src_1.getSectionsAtPhysicalMemoryLocation(elf, Number(address) + Number(section.size) - 1);
        assert_1.default(sections);
        assert_1.default(sections.includes(section));
    });
    _1.test(`getSegmentsAtVirtualMemoryLocation works when queried at start of section`, async () => {
        const elf = await load(armelfdata);
        const segment = elf.segments[0];
        const segments = src_1.getSegmentsAtVirtualMemoryLocation(elf, Number(segment.vaddr));
        assert_1.default(segments);
        assert_1.default(segments.includes(segment));
    });
    _1.test(`getSegmentsAtVirtualMemoryLocation works when queried in middle of section`, async () => {
        const elf = await load(armelfdata);
        const segment = elf.segments[0];
        const segments = src_1.getSegmentsAtVirtualMemoryLocation(elf, Number(segment.vaddr) + 10);
        assert_1.default(segments);
        assert_1.default(segments.includes(segment));
    });
    _1.test(`getSegmentsAtVirtualMemoryLocation works when queried at end of section`, async () => {
        const elf = await load(armelfdata);
        const segment = elf.segments[0];
        const segments = src_1.getSegmentsAtVirtualMemoryLocation(elf, Number(segment.vaddr) + Number(segment.memsz) - 1);
        assert_1.default(segments);
        assert_1.default(segments.includes(segment));
    });
    _1.test(`getSegmentsAtPhysicalMemoryLocation works when queried at start of section`, async () => {
        const elf = await load(armelfdata);
        const segment = elf.segments[0];
        const segments = src_1.getSegmentsAtPhysicalMemoryLocation(elf, Number(segment.paddr));
        assert_1.default(segments);
        assert_1.default(segments.includes(segment));
    });
    _1.test(`getSegmentsAtPhysicalMemoryLocation works when queried in middle of section`, async () => {
        const elf = await load(armelfdata);
        const segment = elf.segments[0];
        const segments = src_1.getSegmentsAtPhysicalMemoryLocation(elf, Number(segment.paddr) + 10);
        assert_1.default(segments);
        assert_1.default(segments.includes(segment));
    });
    _1.test(`getSegmentsAtPhysicalMemoryLocation works when queried at end of section`, async () => {
        const elf = await load(armelfdata);
        const segment = elf.segments[0];
        const segments = src_1.getSegmentsAtPhysicalMemoryLocation(elf, Number(segment.paddr) + Number(segment.memsz) - 1);
        assert_1.default(segments);
        assert_1.default(segments.includes(segment));
    });
    _1.test(`virtualAddressToPhysical works`, async () => {
        const elf = await load(armelfdata);
        // the second segment in ARM has different physical and virtual addresses
        const { vaddr, paddr } = elf.segments[1];
        const testaddr_phys = Number(paddr) + 10;
        const testaddr_virt = Number(vaddr) + 10;
        const result = src_1.virtualAddressToPhysical(elf, testaddr_virt);
        assert_1.default(result === testaddr_phys);
    });
    _1.test(`virtualAddressToFileOffset works`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'globaldatastring');
        assert_1.default(symbol);
        const result = src_1.virtualAddressToFileOffset(elf, symbol.value);
        assert_1.default(result);
        assert_1.default(result > Number(elf.segments[0].offset));
        const symbolvalue = armelfdata.slice(Number(result), Number(result) + Number(symbol.size)).toString();
        assert_1.default(symbolvalue === 'Hello World #X\0');
    });
    _1.test(`physicalAddressToVirtual works`, async () => {
        const elf = await load(armelfdata);
        // the second segment in ARM has different physical and virtual addresses
        const { vaddr, paddr } = elf.segments[1];
        const testaddr_phys = Number(paddr) + 10;
        const testaddr_virt = Number(vaddr) + 10;
        const result = src_1.physicalAddressToVirtual(elf, testaddr_phys);
        assert_1.default(result === testaddr_virt);
    });
    _1.test(`physicalAddressToFileOffset works`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'globalrodatastring');
        assert_1.default(symbol);
        const physicalAddress = src_1.virtualAddressToPhysical(elf, symbol.value);
        assert_1.default(physicalAddress);
        const result = src_1.physicalAddressToFileOffset(elf, physicalAddress);
        assert_1.default(result);
        assert_1.default(result > Number(elf.segments[0].offset));
        const symbolvalue = armelfdata.slice(Number(result), Number(result) + Number(symbol.size)).toString();
        assert_1.default(symbolvalue === 'Hello World\0');
    });
    _1.test(`fileOffsetToPhysicalAddress works`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'globalrodatastring');
        assert_1.default(symbol);
        const fileOffset = src_1.virtualAddressToFileOffset(elf, symbol.value);
        assert_1.default(fileOffset);
        const physicalAddress = src_1.virtualAddressToPhysical(elf, symbol.value);
        const physicalAddress2 = src_1.fileOffsetToPhysicalAddress(elf, fileOffset);
        assert_1.default(physicalAddress2 === physicalAddress);
    });
    _1.test(`fileOffsetToVirtualAddress works`, async () => {
        const elf = await load(armelfdata);
        const symbol = src_1.getSymbolByName(elf, 'globalrodatastring');
        assert_1.default(symbol);
        const fileOffset = src_1.virtualAddressToFileOffset(elf, symbol.value);
        assert_1.default(fileOffset);
        const virtualAddress = symbol.value;
        const virtualAddress2 = src_1.fileOffsetToVirtualAddress(elf, fileOffset);
        assert_1.default(virtualAddress2 === virtualAddress2);
    });
    _1.test(`not all virtual addresses have a corresponding physical address`, async () => {
        const elf = await load(armelfdata);
        // BSS section has a virtual address but not physical
        const section = src_1.getSectionByName(elf, '.bss');
        const testaddr_virt = Number(section.addr) + 10;
        const result = src_1.virtualAddressToPhysical(elf, testaddr_virt);
        assert_1.default(!result);
    });
    _1.test(`getSectionByName works`, async () => {
        const elf = await load(elfdata);
        const section = src_1.getSectionByName(elf, '.text');
        assert_1.default(section);
        assert_1.default(section.name === '.text');
    });
    _1.test(`getSectionsByName works`, async () => {
        const elf = await load(elfdata);
        const sections = src_1.getSectionsByName(elf, '.text');
        assert_1.default(sections);
        assert_1.default(sections.length === 1);
        assert_1.default(sections[0].name === '.text');
    });
    _1.test(`getSymbolByName works`, async () => {
        const elf = await load(elfdata);
        const sym = src_1.getSymbolByName(elf, '__init_array_start');
        assert_1.default(sym);
        assert_1.default(sym.name === '__init_array_start');
        assert_1.default(sym.type === src_1.SymbolType.None);
        assert_1.default(sym.value > 0);
    });
    _1.test(`getSymbolByName works for dynsym`, async () => {
        const elf = await load(elfdata);
        const sym = src_1.getSymbolByName(elf, 'printf');
        assert_1.default(sym);
        assert_1.default(sym.name === 'printf');
        assert_1.default(sym.type === src_1.SymbolType.Function);
        assert_1.default(Number(sym.value) === 0);
    });
});
//# sourceMappingURL=functions.test.js.map