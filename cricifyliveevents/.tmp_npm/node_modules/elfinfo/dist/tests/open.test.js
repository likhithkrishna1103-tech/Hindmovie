"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const elfinfo = __importStar(require("../src"));
const fs_1 = __importDefault(require("fs"));
const testprograms_1 = require("./testprograms");
const _1 = require("./");
_1.category("Open types", () => {
    const data = testprograms_1.testPrograms['clang-x64'].program;
    const path = testprograms_1.testPrograms['clang-x64'].programPath;
    _1.test(`should open a path without problems`, async () => {
        const elf = await elfinfo.open(path);
        elf.warnings.forEach(w => console.warn(w));
        elf.errors.forEach(e => console.error(e));
        assert_1.default(elf.success === true);
        assert_1.default(elf.errors.length === 0);
        assert_1.default(elf.warnings.length === 0);
    });
    _1.test(`should open a sync file handle without problems`, async () => {
        const fh = fs_1.default.openSync(path, 'r');
        const elf = await elfinfo.open(fh);
        fs_1.default.closeSync(fh);
        elf.warnings.forEach(w => console.warn(w));
        elf.errors.forEach(e => console.error(e));
        assert_1.default(elf.success === true);
        assert_1.default(elf.errors.length === 0);
        assert_1.default(elf.warnings.length === 0);
    });
    _1.test(`should open an async file handle without problems`, async () => {
        const fh = await fs_1.default.promises.open(path, 'r');
        const elf = await elfinfo.open(fh);
        await fh.close();
        elf.warnings.forEach(w => console.warn(w));
        elf.errors.forEach(e => console.error(e));
        assert_1.default(elf.success === true);
        assert_1.default(elf.errors.length === 0);
        assert_1.default(elf.warnings.length === 0);
    });
    _1.test(`should open a buffer without problems`, async () => {
        const fh = await fs_1.default.promises.open(path, 'r');
        const buffer = await fh.readFile();
        await fh.close();
        const elf = await elfinfo.open(buffer);
        elf.warnings.forEach(w => console.warn(w));
        elf.errors.forEach(e => console.error(e));
        assert_1.default(elf.success === true);
        assert_1.default(elf.errors.length === 0);
        assert_1.default(elf.warnings.length === 0);
    });
    _1.test(`should open an ArrayBuffer without problems`, async () => {
        const fh = await fs_1.default.promises.open(path, 'r');
        const buffer = await fh.readFile();
        await fh.close();
        const elf = await elfinfo.open(buffer.buffer.slice(buffer.byteOffset, buffer.byteLength));
        elf.warnings.forEach(w => console.warn(w));
        elf.errors.forEach(e => console.error(e));
        assert_1.default(elf.success === true);
        assert_1.default(elf.errors.length === 0);
        assert_1.default(elf.warnings.length === 0);
    });
    _1.test(`should open a structured array without problems`, async () => {
        const elf = await elfinfo.open(data);
        elf.warnings.forEach(w => console.warn(w));
        elf.errors.forEach(e => console.error(e));
        assert_1.default(elf.success === true);
        assert_1.default(elf.errors.length === 0);
        assert_1.default(elf.warnings.length === 0);
    });
    _1.test(`should open an ustructured array without problems`, async () => {
        const fh = await fs_1.default.promises.open(path, 'r');
        const buffer = await fh.readFile();
        await fh.close();
        const elf = await elfinfo.open(Array.from(data));
        elf.warnings.forEach(w => console.warn(w));
        elf.errors.forEach(e => console.error(e));
        assert_1.default(elf.success === true);
        assert_1.default(elf.errors.length === 0);
        assert_1.default(elf.warnings.length === 0);
    });
    _1.test(`should open a Blob without problems`, async () => {
        class MyBlob {
            constructor(buffer, offset, length) {
                this.buffer = buffer.slice(offset, length);
                this.size = length;
            }
            arrayBuffer() {
                return Promise.resolve(this.buffer);
            }
        }
        const fh = await fs_1.default.promises.open(path, 'r');
        const buffer = await fh.readFile();
        await fh.close();
        const elf = await elfinfo.open(new MyBlob(buffer.buffer, buffer.byteOffset, buffer.byteLength));
        elf.warnings.forEach(w => console.warn(w));
        elf.errors.forEach(e => console.error(e));
        assert_1.default(elf.success === true);
        assert_1.default(elf.errors.length === 0);
        assert_1.default(elf.warnings.length === 0);
    });
});
//# sourceMappingURL=open.test.js.map