"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const src_1 = require("../src");
const testprograms_1 = require("./testprograms");
const _1 = require("./");
_1.category("Symbols", () => {
    _1.test(`data should be undefined by default`, async () => {
        const elf = await src_1.open(testprograms_1.testPrograms['clang-x64'].program);
        assert_1.default(elf);
        assert_1.default(elf.elf);
        const symbols = src_1.getSymbols(elf.elf);
        symbols.forEach(x => assert_1.default(x.data === undefined));
    });
    _1.test(`data should be loaded for some symbols if specified`, async () => {
        const elf = await src_1.open(testprograms_1.testPrograms['clang-x64'].program, { readSymbolData: true });
        assert_1.default(elf);
        assert_1.default(elf.elf);
        const symbols = src_1.getSymbols(elf.elf);
        assert_1.default(symbols && symbols.length);
        let symbolsWithData = 0;
        symbols.forEach(x => symbolsWithData += x.data ? 1 : 0);
        assert_1.default(symbolsWithData !== 0);
    });
    _1.test(`all symbols with data should be loaded`, async () => {
        const elf = await src_1.open(testprograms_1.testPrograms['clang-x64'].program, { readSymbolData: true });
        assert_1.default(elf);
        assert_1.default(elf.elf);
        const symbols = src_1.getSymbols(elf.elf);
        assert_1.default(symbols && symbols.length);
        let symbolDataLengthTotal = 0;
        let symbolSizeTotal = 0;
        symbols.forEach(x => {
            if (x.type === src_1.SymbolType.Object || x.type === src_1.SymbolType.Function) {
                if (elf.elf && x.shndx < elf.elf.sections.length &&
                    elf.elf.sections[x.shndx].type != src_1.SectionHeaderEntryType.NoBits) {
                    symbolSizeTotal += Number(x.size);
                }
            }
            if (x.data) {
                symbolDataLengthTotal += x.data.length;
            }
        });
        assert_1.default(symbolDataLengthTotal === symbolSizeTotal);
    });
});
//# sourceMappingURL=symbols.test.js.map